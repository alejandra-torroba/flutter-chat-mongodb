name: chat_mongodb

description: En esta aplicación utilizare una base de datos MongoDB para hacer un chat en linea

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=2.19.1 <3.0.0'

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.2
  pull_to_refresh: ^2.0.0
  provider: ^6.0.5
  http: ^0.13.5
  flutter_secure_storage: ^8.0.0
  socket_io_client: ^2.0.1

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^2.0.0


flutter:

  uses-material-design: true

  assets:
    - assets/

